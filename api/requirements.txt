# Web framework
fastapi==0.115.12
uvicorn[standard]==0.34.3
python-multipart==0.0.20
pydantic==2.11.4

# Database
sqlalchemy[asyncio]==2.0.41
asyncpg==0.30.0

# Permalink ID generation
nanoid==2.0.0

# EIS analysis (core — these pull in numpy, scipy, pandas as transitive deps)
impedance                # Circuit fitting, KK validation, file parsing
pyDRTtools               # DRT analysis (Tikhonov, Bayesian, BHT)
galvani                  # Biologic .mpr file parsing

# pyDRTtools transitive deps that need explicit install
cvxopt                   # Convex optimization solver for DRT
scikit-learn             # Used by pyDRTtools parameter selection
